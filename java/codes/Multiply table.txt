package java1;

import java.util.*;

public class age{
	
	static Scanner input = new Scanner(System.in).useLocale(Locale.US);
	
	public static void main(String[] agrs)
	{
		int i, num, x;
		
		System.out.print("Enter any number : ");
		num = input.nextInt();
		
		System.out.println("Multiply table for ( " + num + " ) : \n-------------------------------");
		for(i = 1; i <= 10; i++)
		{
			x = num * i;
			System.out.println(i + " * " + num + " = " + x);
		}
	}
	
}